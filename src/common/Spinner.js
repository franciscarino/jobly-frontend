/** Presentational component for spinner. */

import "./Spinner.css";

function Spinner() {
  return (
    <div className="LoadingSpinner">
      Loading ...
    </div>
  );
}

export default Spinner;